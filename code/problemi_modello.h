#ifndef PROBLEMI_MODELLO_H_INCLUDED
#define PROBLEMI_MODELLO_H_INCLUDED


#include "precision.h"
#include "mat.h"


void dati_12(struct Matrix, Real *, Real *);
void problema_12(struct Matrix, Real, struct Matrix);


void dati_22(struct Matrix, Real *, Real *);
void problema_22(struct Matrix, Real, struct Matrix);


#endif // PROBLEMI_MODELLO_H_INCLUDED
